# 部署用配置（复制为 deploy.env 后填写，deploy.env 已加入 .gitignore）
# 前端页面：https://cheng-bgstx.github.io/111/
# CORS 的 Origin 不含路径，只填协议+域名
FRONTEND_ORIGIN=https://cheng-bgstx.github.io
# 与前端 GitHub Secret VITE_API_KEY 保持一致，启用后前后端都会校验
API_KEY=

# 以下为发布前端到 GitHub 时使用（仅运行 run_deploy_frontend.sh 时需要）
GITHUB_USER=Cheng-bgstx
GITHUB_REPO=111
# 必填：Personal access token (repo 权限)，不要提交到仓库
GITHUB_TOKEN=
